# Direct Delivery — Mobile (Phase 2)

Expo Android/iOS app comes **after** the website is verified.

- Same Supabase database as `apps/web`
- Same design tokens (blue / light gold / white / black)
- Same flows: client, driver, admin
- Native push + background location

Do not start this package until Phase 1 web flows pass browser verification.
