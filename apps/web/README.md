# `@direct/web`

Next.js App Router UI for Direct (Phase 1).

Demo logins, roles, stack, and how to run live in the [repository README](../../README.md). From the repo root:

```bash
npm install
npm run dev
```

Then open [http://localhost:3000](http://localhost:3000). Accounts in that README are **demo-only**.
